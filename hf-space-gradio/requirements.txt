# HF Gradio Space deps. NOTE: gradio is PRE-INSTALLED by the SDK (version set by
# sdk_version in README.md), so it is NOT listed here. We add the backend libs.
# fastapi/uvicorn/pydantic/starlette come in via gradio's own dependencies.

# --- backend web extras ---
sse-starlette==2.1.3
python-dotenv==1.0.1
python-multipart==0.0.20

# --- LLM ---
anthropic==0.42.0

# --- ML (Analysis Agent) ---
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
scipy==1.14.1
xgboost==2.1.3
lightgbm==4.5.0
shap==0.46.0
