# HF Gradio Space deps. gradio brings fastapi/uvicorn/pydantic/starlette;
# we add the backend's own libraries. (Kept unpinned for fastapi/pydantic so
# gradio can resolve compatible versions.)
gradio==4.44.1

# --- backend web extras ---
sse-starlette==2.1.3
python-dotenv==1.0.1
python-multipart==0.0.20

# --- LLM ---
anthropic==0.42.0

# --- ML (Analysis Agent) ---
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
scipy==1.14.1
xgboost==2.1.3
lightgbm==4.5.0
shap==0.46.0
